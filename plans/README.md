# Implementation Plans

Generated by the improve skill on 2026-06-12; updated 2026-09-23. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add a first-class `agentctl dashboard` command | P1 | M | — | DONE |
| 002 | Add safe `agentctl prune` (reclaim logs, keep metadata) | P1 | M | — | DONE |
| 003 | Harden dashboard: loopback bind, Host/Origin guard, id validation | P1 | S | — | DONE |
| 004 | Fix duplicated history in dashboard log stream | P1 | S | 003 | DONE |
| 005 | Fix flaky Codex notifier test timeouts | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 has no dependencies.
- Plan 002 has no dependencies and can ship independently of dashboard work.
- Plan 002 deliberately defers hard-delete, pin/keep-last-N, and log compaction/summary fields.
- Plan 004 runs after 003: both edit `internal/dashboard/server.go` / `server_test.go`; 004 expects 003's id check at the top of `handleSessionLogs`.
- Plan 005 is independent.

## Findings considered and rejected

- Separate dashboard release binary: not planned here because the higher-leverage path is embedding the existing dashboard in the already-installed root `agentctl` binary as `agentctl dashboard`.
- Prune as hard history wipe: rejected for v1 — keep session JSON so past tasks/models/costs remain learnable; only reclaim bulky log artifacts for old finished sessions.
- Shell injection in `cmd/run.go` script generation (2026-09-23 quick audit): not an issue — values go through `shellQuote` and the task via a file.
- Dashboard client duplicating logs on SSE auto-reconnect (2026-09-23): rejected — `onerror` closes the EventSource, so there is no auto-reconnect.
